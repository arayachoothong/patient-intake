import { Alert, AlertDescription } from "@patient/ui";

export function ResumeBanner() {
  return (
    <Alert>
      <AlertDescription>Welcome back — resuming your check-in.</AlertDescription>
    </Alert>
  );
}
